


worked on login and register api
    - created schema
    - setup the google api console
    - taking(client_id, client_secret , refresh_token)
    - setup the nodemailer

worked on account api
    - created the account schema with idempotencyKey and indexes
    - account routes setup
    - creating the auth middleware for checked the login user
    - route and controller setup(account)


worked on trasanctions api
    - created the trasanctions schema with indexes
    - created the ledger schema with prevent modification and indexes
    - routes setup
    - worked on the create trasanctions api(validating request , validating idempotencyKey)
    - checked account status(for new request like active)
    - derive sender balance from ledger(calculating the balance in AccountModel)
    - creating the trasanctions and sessions like creadit, debit and end trasanctions commit trasanctions save also status COMPLETED
    - setup the email service for sendTransactionEmail, sendTransactionFailureEmail and send email(sendTransactionEmail)


worked on the system user api(for sending the money to the user accounts to perform the trasanctions)
    - added the field of the systemUser in the userModel
    - now registerd one systemUser (name:system,email:system@test.com , password:test123) and also register one account
    - worked on the middleware(systemUser middleware)

worked on the create Initial Funds Transfer
    - routes setup for the initial fund(/system/initial-funds)
    - controller setup for the create Initial Funds Transfer
    - debit and creadit ledger entries


worked on get accounts of logged in user
    - route setup(protected route)
    - controller setup for getting all accounts of logged-in user


worked on get balance
    - route setup(protected route)
    - controller setup for get account

